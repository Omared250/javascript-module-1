// Declare your function first
function addingTwoNumbers(firstNum, secondNum) {
    return firstNum + secondNum;
}

// Call the function and assign to a variable `sum`
var sum = addingTwoNumbers(13, 124);
console.log(sum);
